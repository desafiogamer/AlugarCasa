import { Routes } from '@angular/router';
import { InicioComponent } from './modules/homeSpace/pages/inicio/inicio.component';

export const routes: Routes = [
    {
      path: '',
      component: InicioComponent
    }
];
